; 实验4（3）下面程序的功能是将“MOV AX, 4C00H”之前的指令复制到内存0:200处
ASSUME CS:CODE

CODE SEGMENT

COPYSTART:
	MOV AX, CS
	MOV DS, AX
	MOV AX, 0020H
	MOV ES, AX
	MOV BX, 000H
	
	MOV CX, OFFSET COPYEND - OFFSET COPYSTART
	
S:	MOV AL, [BX]
	MOV ES:[BX], AL
	INC BX
	LOOP S
COPYEND:	
	MOV AX, 4C00H
	INT 21H

CODE ENDS
END