DATA 	SEGMENT
		BUF		DW		0FFFFH
DATA 	ENDS

STACK	SEGMENT	STACK
		DB		128		DUP		(?)
STACK	ENDS

CODE 	SEGMENT
		ASSUME	CS:CODE, DS:DATA
START:	MOV		AX, DATA
		MOV		DS, AX
		MOV		AX, BUF
		MOV		BX, 000AH
		MOV		CX, 0000H
DIV_S:	MOV		DX, 0000H
		DIV		BX ; DX:AX / 10
		INC		CX ; 除法计数
		PUSH 	DX ; 余数入栈
		CMP		AX, 00H
		JNZ		DIV_S
DIS_S:	POP		DX ; 余数出栈
		ADD		DL, 30H
		MOV		AH, 02H
		INT 	21H
		LOOP	DIS_S
		
		MOV		AX, 4C00H
		INT		21H
CODE	ENDS
		END	START