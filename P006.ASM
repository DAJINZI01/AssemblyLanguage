; 将内存单元FFFF0 - FFFFC 中的数累加到DX中
ASSUME CS:CODESG

CODESG SEGMENT
START:	MOV AX, 0FFFFH
		MOV DS, AX
		MOV BX, 0000H
		MOV DX, 0000H
		
		; 循环开始
		MOV CX, 000CH
S:		MOV AL, [BX]
		MOV AH, 00H
		ADD DX, AX
		INC BX
		LOOP S
		
		; 程序返回
		MOV AX, 4C00H
		INT 21H
CODESG ENDS
END